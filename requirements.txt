# Requirements pinned to specific versions for reproducible builds.
# Policy: each package is pinned to its latest stable minor version
# as of May 2026. Use `pip install -r requirements.txt` to install.
# To upgrade, bump the minor version after testing.

aiogram==3.16.0
telethon==1.39.0
SQLAlchemy[asyncio]==2.0.40
aiosqlite==0.20.0
pydantic==2.10.6
pydantic-settings==2.8.0
cryptography==44.0.1
python-dotenv==1.1.0
openai==1.72.0
google-genai==1.2.0
httpx==0.28.1

# Транскрипция
faster-whisper==1.1.1

# Документы
pypdf==5.2.0
python-docx==1.1.2

# Векторный поиск
qdrant-client==1.14.0
pytest>=8.0

# Proxy support (SOCKS5)
aiohttp-socks==0.10.1
python-socks==2.7.2

# Fuzzy matching контактов
rapidfuzz==3.12.2

# YAML frontmatter для метаданных навыков
PyYAML==6.0.2

# Миграции схемы БД
alembic>=1.13
